#include "pawn.h"

namespace NFairyChess::NVanillaPieces {

void TPawnPiece::FillMoves(TMoveContext moveContext) {
    auto& moveContainer = moveContext.MoveContainer;
    auto& board = moveContext.Board;

    // move 1 square forward
}

} // namespace NFairyChess::NVanillaPieces
